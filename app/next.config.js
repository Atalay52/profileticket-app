/** @type {import('next').NextConfig} */
const nextConfig = {
  // Eğer basePath kullanıyorsan burayı doğru ayarla; yoksa kaldır
  // basePath: '/some-basepath',
  // trailingSlash: false,
  reactStrictMode: true,
};

module.exports = nextConfig;
